# Phase 4-5 报告生成策略指南

> ⚠️ 此文档解决 "Context low · Run /compact" 问题的核心机制
> 对应规则 12：Phase 4-5 报告生成策略

---

## 问题根因

```
┌─────────────────────────────────────────────────────────────────────────┐
│  为什么会在 Phase 4 发生 Context 溢出？                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 0-3 已占用的 context（对 Phase 4-5 无价值但仍在内存中）：           │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  - 多次 tavily_search 返回的 JSON (~20K tokens)                  │   │
│  │  - Phase 0 摸底报告和用户确认对话                                   │   │
│  │  - Phase 2 所有 prompt 文件的创建过程                               │   │
│  │  - Phase 3 的 7 个子代理启动和状态监控                              │   │
│  │  - 所有中间对话历史                                                │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  Phase 4 尝试读取的新内容：                                              │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  - subtask_01.md (441 lines, 19KB)                              │   │
│  │  - subtask_02.md (406 lines, 15KB)                              │   │
│  │  - subtask_03.md (326 lines, 14KB)                              │   │
│  │  - subtask_04.md (344 lines, 13KB)                              │   │
│  │  - subtask_05.md (344 lines, 8KB)                               │   │
│  │  - subtask_06.md (303 lines, 15KB)                              │   │
│  │  - subtask_07.md (271 lines, 12KB)                              │   │
│  │  ───────────────────────────────────────                        │   │
│  │  总计: ~2435 lines, ~96KB                                       │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
│  结果：历史 context + 新读取内容 → 超过 context 限制 → 溢出！            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 核心洞察

```
┌─────────────────────────────────────────────────────────────────────────┐
│  💡 关键洞察                                                             │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. Phase 0-3 的历史对 Phase 4-5 已无价值                               │
│     → 但它们仍占用主 Agent 的 context                                   │
│                                                                         │
│  2. 子代理启动时拥有全新的 context 空间                                   │
│     → 不受主 Agent 历史影响                                             │
│     → 可以读取所有需要的内容                                             │
│                                                                         │
│  3. 主 Agent 不需要看到报告的完整内容也能完成任务                          │
│     → 只需要结构化的验收清单                                             │
│     → 用户可以直接查看生成的文件                                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 策略选择

```
┌─────────────────────────────────────────────────────────────────────────┐
│  根据子任务数量选择策略                                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  子任务数量                                                              │
│      │                                                                  │
│      ├── ≤3 个 ──────→ 策略 C: 主 Agent 直接处理                        │
│      │                 输出文件总量小，通常不会溢出                        │
│      │                                                                  │
│      ├── 4-6 个 ─────→ 策略 A: Report-Generator 子代理（推荐）           │
│      │                 安全高效，自动化程度高                             │
│      │                                                                  │
│      └── ≥7 个 ──────→ 策略 A（必须）或 策略 B                          │
│                        必须使用隔离策略，否则必然溢出                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 策略 A：Report-Generator 子代理模式（推荐）

### 核心原理

```
子代理启动时拥有全新的 context 空间！

这意味着：
- Report-Generator 子代理可以读取所有 outputs/*.md
- 不受主 Agent 历史 context 的影响
- 有足够空间执行完整的 Phase 4-5
```

### 执行流程图

```
┌─────────────────────────────────────────────────────────────────────────┐
│                                                                         │
│  主 Agent                        Report-Generator 子代理               │
│  ────────                        ───────────────────────               │
│      │                                                                  │
│      │ 1. Phase 3 完成，确认子任务输出文件存在                            │
│      │    Bash("ls -la $WORK_DIR/outputs/")                            │
│      │    ⚠️ 不读取文件内容！                                           │
│      │                                                                  │
│      │ 2. 更新 checkpoint.json                                          │
│      │    标记 Phase 4 开始                                             │
│      │                                                                  │
│      │ 3. 启动 Report-Generator 子代理                                  │
│      │    ─────────────────────────────→  ┌────────────────────────┐   │
│      │                                    │ ✅ 全新 context 空间！   │   │
│      │                                    │                          │   │
│      │                                    │ 4. 读取所有 outputs/*.md │   │
│      │                                    │                          │   │
│      │                                    │ 5. Phase 4: 聚合整理     │   │
│      │                                    │    - 保留所有证据链       │   │
│      │                                    │    - 按主题重新组织       │   │
│      │                                    │    - 生成 aggregated_raw │   │
│      │                                    │                          │   │
│      │                                    │ 6. Phase 5: 润色报告     │   │
│      │                                    │    - 三层报告结构         │   │
│      │                                    │    - 逐章撰写             │   │
│      │                                    │    - 生成 final_report   │   │
│      │                                    │                          │   │
│      │                                    │ 7. Phase 5.5: 自审验证   │   │
│      │                                    │    - 证据链保留率 ≥30%   │   │
│      │                                    │    - 证据密度 ≥2.0       │   │
│      │                                    │    - 数据完整性验证       │   │
│      │                                    └────────────────────────┘   │
│      │                                              │                   │
│      │ 8. 接收验收清单 ←───────────────────────────┘                   │
│      │    (结构化摘要 ≤800字)                                          │
│      │                                                                  │
│      │ 9. 更新 checkpoint.json                                          │
│      │    标记任务完成                                                   │
│      │                                                                  │
│      │ 10. 向用户展示验收清单                                            │
│      │     - 核心发现摘要                                                │
│      │     - 可执行建议                                                  │
│      │     - 文件路径                                                    │
│      ▼                                                                  │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Report-Generator 子代理完整 Prompt

```markdown
# 报告生成任务

## 任务类型
你是 Report-Generator 子代理，负责完成 Wide Research 的 Phase 4-5。
你拥有全新的 context 空间，可以读取所有子任务输出文件。

## 工作目录
$WORK_DIR = "{{WORK_DIR}}"

## 输入文件
$WORK_DIR/outputs/*.md（共 {{SUBTASK_COUNT}} 个子任务输出）

## 执行步骤

### Step 1: 读取所有输出文件
依次读取所有 outputs/*.md 文件，理解每个子任务的发现和证据。

### Step 2: Phase 4 聚合
- 保留所有原文摘录（不压缩证据链）
- 按主题/维度重新组织内容
- 标记失败或不完整的子任务
- 生成聚合文件

输出文件：$WORK_DIR/aggregated_raw.md

### Step 3: Phase 5 润色
- 使用三层报告结构（核心洞察 → 证据支撑 → 完整素材）
- 设计章节大纲（根据调研维度）
- 逐章撰写精修稿
- 生成最终报告

输出文件：$WORK_DIR/final_report.{{FORMAT}}

### Step 4: Phase 5.5 自审验证
验证以下指标：

| 检查项 | 目标值 | 计算方式 |
|-------|-------|---------|
| 证据链保留率 | ≥30% | 报告原文数 / 子代理原文总数 |
| 报告证据密度 | ≥2.0 | 报告原文数 / 核心发现数 |
| 数据完整性 | 100% | 显示条目数 = 数据条目数 |

如不通过，使用三层结构（含可折叠证据区域）重写。

输出文件：$WORK_DIR/evidence_summary.md（证据汇总表）

## 🔴 输出文件清单【强制，缺一不可】

必须生成以下 3 个文件，否则任务失败：

```
⛔ 重要：所有输出文件必须在 $WORK_DIR/ 根目录！
⛔ 禁止写入 $WORK_DIR/outputs/ 目录！

正确路径：
  ✅ $WORK_DIR/aggregated_raw.md
  ✅ $WORK_DIR/final_report.md
  ✅ $WORK_DIR/evidence_summary.md

错误路径：
  ❌ $WORK_DIR/outputs/aggregated_raw.md
  ❌ $WORK_DIR/outputs/final_report.md
```

| # | 文件（根目录） | 说明 |
|---|---------------|------|
| 1 | `$WORK_DIR/aggregated_raw.md` | 完整聚合（保留所有证据链） |
| 2 | `$WORK_DIR/final_report.md` | 最终报告 |
| 3 | `$WORK_DIR/evidence_summary.md` | 证据汇总表 |

**⛔ 完成后必须执行验证**：
```bash
# 验证 3 文件在根目录存在（不是 outputs/）
cd $WORK_DIR && \
  [ -f aggregated_raw.md ] && echo "✅ aggregated_raw.md" || echo "❌ 缺失" && \
  [ -f final_report.md ] && echo "✅ final_report.md" || echo "❌ 缺失" && \
  [ -f evidence_summary.md ] && echo "✅ evidence_summary.md" || echo "❌ 缺失"
```

## 返回格式【强制遵守】

完成后，只返回以下结构化验收清单（≤800字）：

---

## ✅ Wide Research 报告生成完成

### 验收指标（含计算过程）

| 指标 | 计算公式 | 原始数据 | 结果 | 阈值 | 状态 |
|-----|---------|---------|-----|-----|-----|
| 证据链保留率 | 报告原文数/子代理原文总数 | X/Y | Z% | ≥30% | ✅/❌ |
| 报告证据密度 | 报告原文数/核心发现数 | X/Y | Z.Z | ≥2.0 | ✅/❌ |
| 数据完整性 | 显示条目数=数据条目数 | X=Y | 100% | 100% | ✅/❌ |

- 自审通过: ✅/❌

### 核心发现摘要（≤5条）
1. [发现1 - 简洁描述]
2. [发现2 - 简洁描述]
3. [发现3 - 简洁描述]
4. [发现4 - 简洁描述]
5. [发现5 - 简洁描述]

### 可执行建议
- [建议1]
- [建议2]

### 建议后续
| 深入方向 | 原因 | 触发方式 |
|---------|------|---------|
| [方向1] | [原因] | 说"深入调研 X" |
| [方向2] | [原因] | 说"对比 X 和 Y" |

### 文件位置
- 完整报告: $WORK_DIR/final_report.{{FORMAT}}
- 聚合数据: $WORK_DIR/aggregated_raw.md
- 证据汇总: $WORK_DIR/evidence_summary.md

---

⛔ 禁止在返回消息中包含完整报告内容！验收清单必须 ≤800 字。
```

### 主 Agent 执行代码

```javascript
// Phase 4-5 正确执行方式（≥4 个子任务）

// Step 1: 检查子任务完成状态（不读取内容！）
Bash(`
  ls -la ${WORK_DIR}/outputs/
  COMPLETED=$(ls ${WORK_DIR}/outputs/*.md 2>/dev/null | wc -l)
  echo "✅ 已完成子任务: $COMPLETED 个"
`)

// Step 2: 更新检查点
Bash(`
  cat > ${WORK_DIR}/logs/checkpoint.json << 'EOF'
  {
    "current_phase": 4,
    "phase_status": {
      "phase_0": "completed",
      "phase_1": "completed",
      "phase_2": "completed",
      "phase_3": "completed",
      "phase_4": "in_progress",
      "phase_5": "pending"
    },
    "report_generator": "starting",
    "last_update": "${ISO_TIME}",
    "recovery_hint": "Phase 4-5 使用 Report-Generator 子代理处理"
  }
  EOF
`)

// Step 3: 启动 Report-Generator 子代理
Task("生成最终报告", `
  # 报告生成任务

  ## 工作目录
  $WORK_DIR = "${WORK_DIR}"

  ## 任务职责
  完成 Phase 4-5：读取所有输出、聚合、润色、自审、生成最终报告

  ## 输入
  $WORK_DIR/outputs/*.md（${SUBTASK_COUNT} 个文件）

  ## 输出
  1. aggregated_raw.md
  2. final_report.html（或 .md）
  3. evidence_summary.md

  ## 返回
  只返回结构化验收清单（≤800字），包含核心发现、建议、文件路径
  禁止返回完整报告内容！
`, "researcher", { model: "sonnet" })  // 报告生成建议用 sonnet 提升质量

// Step 4: 接收验收清单，直接展示给用户
// 子代理返回的验收清单已包含用户需要的所有信息
```

---

## 策略 B：主动 Compact 模式

### 适用场景

- 用户明确希望主 Agent 亲自审阅报告内容
- 需要主 Agent 对报告进行多轮修改
- 任务复杂度高，需要主 Agent 的高阶推理

### 执行流程

```
┌─────────────────────────────────────────────────────────────────────────┐
│  主动 Compact 模式                                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 3 完成后                                                         │
│      │                                                                  │
│      ▼                                                                  │
│  1. 确保检查点完整保存                                                   │
│     - checkpoint.json 记录所有状态                                       │
│     - 所有 outputs/*.md 已写入                                          │
│      │                                                                  │
│      ▼                                                                  │
│  2. 主 Agent 提示用户                                                    │
│     ┌──────────────────────────────────────────────────────────────┐   │
│     │ "Phase 3 已完成，7 个子任务输出文件共 ~2400 行。               │   │
│     │  如果您希望我亲自审阅并生成报告，建议执行 /compact 清理历史。   │   │
│     │  这将释放 context 空间，让我能够读取所有内容。                  │   │
│     │                                                                │   │
│     │  选项：                                                        │   │
│     │  A. 执行 /compact 后继续（我将亲自处理）                       │   │
│     │  B. 使用子代理自动生成报告（推荐，更快）"                       │   │
│     └──────────────────────────────────────────────────────────────┘   │
│      │                                                                  │
│      ▼                                                                  │
│  3. 用户执行 /compact                                                   │
│      │                                                                  │
│      ▼                                                                  │
│  4. Compact 后主 Agent                                                  │
│     - 读取 checkpoint.json 恢复状态                                     │
│     - 现在有足够空间读取所有 outputs/*.md                               │
│     - 亲自执行 Phase 4-5                                               │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### Compact 后恢复代码

```javascript
// /compact 后主 Agent 恢复执行

// Step 1: 读取检查点
Read("${WORK_DIR}/logs/checkpoint.json")

// Step 2: 确认状态
// checkpoint 显示 phase_3: completed，可以继续 Phase 4

// Step 3: 现在可以读取所有输出文件
Read("${WORK_DIR}/outputs/subtask_01.md")
Read("${WORK_DIR}/outputs/subtask_02.md")
// ... 读取所有文件

// Step 4: 执行 Phase 4 聚合
// ...

// Step 5: 执行 Phase 5 润色
// ...
```

---

## 策略 C：直接处理模式（≤3 个子任务）

### 适用场景

- 子任务数量 ≤3 个
- 输出文件总量较小（<1000 行）
- 主 Agent context 仍有充足空间

### 判断条件

```javascript
// 在 Phase 3 完成后评估
const subtaskCount = fs.readdirSync(`${WORK_DIR}/outputs/`).length;
const totalLines = // 计算所有文件行数

if (subtaskCount <= 3 && totalLines < 1000) {
  // 策略 C: 主 Agent 直接处理
} else {
  // 策略 A: 使用 Report-Generator 子代理
}
```

---

## 检查点更新时机

```
┌─────────────────────────────────────────────────────────────────────────┐
│  检查点必须在以下时机更新                                                 │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  Phase 3 完成后（启动 Phase 4 前）                                       │
│  {                                                                      │
│    "current_phase": 4,                                                  │
│    "phase_3": "completed",                                              │
│    "phase_4": "in_progress",                                            │
│    "report_generator": "starting",                                      │
│    "recovery_hint": "准备启动 Report-Generator 子代理"                   │
│  }                                                                      │
│                                                                         │
│  Report-Generator 子代理启动后                                           │
│  {                                                                      │
│    "report_generator": "running",                                       │
│    "recovery_hint": "Report-Generator 正在处理 Phase 4-5"               │
│  }                                                                      │
│                                                                         │
│  报告生成完成后                                                          │
│  {                                                                      │
│    "current_phase": 5,                                                  │
│    "phase_4": "completed",                                              │
│    "phase_5": "completed",                                              │
│    "report_generator": "completed",                                     │
│    "final_report": "final_report.html",                                 │
│    "recovery_hint": "任务完成"                                          │
│  }                                                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## Auto-Compaction 恢复流程

如果在 Phase 4-5 期间发生 auto-compaction：

```markdown
## 恢复步骤

1. 读取 checkpoint.json
   - 检查 current_phase
   - 检查 report_generator 状态

2. 根据状态恢复
   - 如果 report_generator: "starting" → 重新启动子代理
   - 如果 report_generator: "running" → 检查输出文件是否已生成
   - 如果 report_generator: "completed" → 读取验收清单展示给用户

3. 检查输出文件
   - 如果 final_report 已存在 → 直接展示结果
   - 如果 aggregated_raw 已存在 → 只需启动 Phase 5 润色
   - 如果都不存在 → 重新启动完整流程
```

---

## 常见问题

### Q: 主 Agent 不读取报告内容，如何保证质量？

**A**: Report-Generator 子代理返回的验收清单包含：
- 量化的质量指标（证据链保留率、证据密度）
- 核心发现摘要（快速了解报告内容）
- 自审结果（确认通过各项检查）

主 Agent 通过验收清单判断质量是否达标。用户可以直接查看生成的文件获取完整内容。

### Q: 如果验收清单显示质量不达标怎么办？

**A**: 主 Agent 可以：
1. 给子代理发送修改指令
2. 使用策略 B（主动 compact）亲自处理
3. 提示用户问题所在，询问是否继续

### Q: 为什么建议 Report-Generator 使用 sonnet 而非 haiku？

**A**: Phase 4-5 涉及：
- 跨子任务的信息整合
- 报告结构设计
- 高质量文字润色

这些任务需要更强的推理和写作能力，sonnet 更适合。而 Phase 3 的子代理使用 haiku 是因为它们只需要定向检索和信息提取。

---

## 总结

```
┌─────────────────────────────────────────────────────────────────────────┐
│  核心原则                                                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. 利用子代理的独立 context 空间处理大量内容                              │
│                                                                         │
│  2. 主 Agent 只接收结构化的验收清单，不读取完整报告                        │
│                                                                         │
│  3. 检查点必须及时更新，确保可恢复                                         │
│                                                                         │
│  4. 根据任务规模自动选择策略                                              │
│                                                                         │
│  5. 提供策略 B 作为用户可选的备选方案                                      │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```
